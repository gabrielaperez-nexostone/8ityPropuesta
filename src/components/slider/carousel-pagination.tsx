export function CarouselPagination({ count, current, goTo }: { count: number; current: number; goTo: (index: number) => void }) { return <div className="flex items-center gap-2" role="tablist" aria-label="Paginación del carrusel">{Array.from({ length: count }, (_, index) => <button key={index} onClick={() => goTo(index)} className={`h-2 rounded-full transition-all ${current === index ? "w-7 bg-primary" : "w-2 bg-border"}`} aria-label={`Ir a diapositiva ${index + 1}`} aria-current={current === index ? "true" : undefined} />)}</div>; }
