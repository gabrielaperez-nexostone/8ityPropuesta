"use client";
import { Dialog as Primitive } from "radix-ui";
import { Button } from "./button";
import { useLanguage } from "@/providers/language-provider";
export function Dialog({ trigger }: { trigger: React.ReactNode }) { const { language } = useLanguage(); const text = language === "es" ? { title: "Todo el negocio, conectado", description: "8ity reúne finanzas, CRM, proyectos, documentos, automatizaciones y agentes de IA para que tu equipo trabaje con una sola fuente de contexto.", close: "Cerrar" } : { title: "Your entire business, connected", description: "8ity brings finance, CRM, projects, documents, automations and AI agents together so your team can work from one shared source of context.", close: "Close" }; return <Primitive.Root><Primitive.Trigger asChild>{trigger}</Primitive.Trigger><Primitive.Portal><Primitive.Overlay className="fixed inset-0 z-50 bg-black/70 backdrop-blur-sm" /><Primitive.Content className="fixed left-1/2 top-1/2 z-50 w-[calc(100%-2rem)] max-w-lg -translate-x-1/2 -translate-y-1/2 rounded-[1.75rem] border border-border bg-surface p-6 shadow-2xl sm:p-8"><Primitive.Title className="text-2xl font-semibold tracking-tight">{text.title}</Primitive.Title><Primitive.Description className="mt-3 leading-7 text-muted-foreground">{text.description}</Primitive.Description><div className="mt-7 flex justify-end"><Primitive.Close asChild><Button>{text.close}</Button></Primitive.Close></div><Primitive.Close className="absolute right-5 top-5 grid size-9 place-items-center rounded-full hover:bg-primary-soft" aria-label={text.close}>×</Primitive.Close></Primitive.Content></Primitive.Portal></Primitive.Root>; }
